import React from 'react';
import './header.css';

const Header = () => {
    return (
        <header className="header row">
            <a className="logo text-dark" href="#">eDuty</a>
        </header>
    );
};

export default Header;